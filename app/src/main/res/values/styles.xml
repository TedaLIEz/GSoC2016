<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="SearchActivityStyle" parent="AppTheme.NoActionBar">
        <item name="colorPrimary">@color/search_navi</item>
        <item name="colorPrimaryDark">@color/search_navi_dark</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.SearchActionBar" parent="AppTheme.PopupOverlay">
        <item name="android:background">@color/search_navi</item>
    </style>

    <style name="FabMenuStyle">
        <item name="android:background">?attr/fabLabelBackgroundDrawable</item>
        <item name="android:textColor">?attr/colorFabText</item>
    </style>

    <style name="Dialog.FullScreen" parent="Theme.AppCompat.Dialog">
        <item name="android:padding">0dp</item>
    </style>

</resources>
